image: maven:3.6.3

pipelines:
  default:
    - step:
        name: Build and Package
        caches:
          - maven
        script:
          - mvn clean compile package
        artifacts:
          - target/**

definitions:
  caches:
    maven: ~/.m2
